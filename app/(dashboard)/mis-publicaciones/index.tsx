import { View } from 'react-native';
import Publications from '@/components/tables/Publications';

export default function VerPublicaciones() {
  return (
    <View className="flex-1 bg-gray-50">
      <Publications />
    </View>
  );
}