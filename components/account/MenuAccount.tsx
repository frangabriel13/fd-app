import { View, Text, StyleSheet } from 'react-native';

const MenuAccount = () => {
  return (
    <View style={styles.container}>
      <Text>MenuAccount</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 0,
  },
});


export default MenuAccount;