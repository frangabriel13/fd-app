import { View, Text, StyleSheet } from 'react-native';

const UnifyOrder = () => {
  return (
    <View style={styles.container}>
      <Text>UnifyOrder</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 0,
  },
});


export default UnifyOrder;