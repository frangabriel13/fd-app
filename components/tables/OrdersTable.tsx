import { View, Text, StyleSheet } from 'react-native';

const OrdersTable = () => {
  return (
    <View style={styles.container}>
      <Text>OrdersTable</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 0,
  },
});


export default OrdersTable;