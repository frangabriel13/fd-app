import { View, Text, StyleSheet } from 'react-native';

const SubordersTable = () => {
  return (
    <View style={styles.container}>
      <Text>SubordersTable</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 0,
  },
});


export default SubordersTable;